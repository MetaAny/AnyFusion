# Provider settings used only by AnyFusion Planner (Pi fork).
# Copy to docker/planner-pi.env and fill the fixed AnyFusion provider values.
# Never commit real keys.

OPENAI_API_KEY=
OPENAI_BASE_URL=

PI_SKIP_VERSION_CHECK=1
PI_TELEMETRY=0
